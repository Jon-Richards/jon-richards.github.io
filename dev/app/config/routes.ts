/** All possible routes for the application. */
export const ROUTES = {
  'home': '/',
  'portfolio_piece': '/portfolio/:piece_id',
  'resume': '/resume',
  'contact': '/contact',
  'error': '/error/:error_code'
};