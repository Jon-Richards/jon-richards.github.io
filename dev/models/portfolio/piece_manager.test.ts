/**
 * @fileoverview
 * Contains tests for the PieceManager class.
 */

import { PieceManager, Piece, PieceShape } from './mediator';

/** Array of pieces for testing. */
describe('The PieceManager class', () => {
    it('Should be two.', () => {
        expect( 1 + 1 ).toBe(2);
    });
});